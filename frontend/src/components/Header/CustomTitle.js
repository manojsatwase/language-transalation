import { worldIcon } from "../../constants";

const CustomIcon = () => (
    <div>
      <img
        src={worldIcon}
        alt="Logo"
        style={{ width: '40px', height: '25px'}} 
      />
    </div>
  );

  export default CustomIcon;